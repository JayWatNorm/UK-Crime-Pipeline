# Copy to .env and fill in real values. .env itself is gitignored.
# Entirely separate from Comfort Compass's .env — different DB, different container.

DB_NAME=crime_data
DB_USER=crime_pipeline
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5433
